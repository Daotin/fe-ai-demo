{
  "name": "fe-ai-demo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "workspaces": [
    "packages/*"
  ]
}
