{
  "name": "fe-ai-demo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.4.4"
  },
  "packageManager": "pnpm@8.5.0"
}
